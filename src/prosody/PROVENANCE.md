# Provenance

Vendored verbatim from `peerapongsm/klon` (project-44-klon), commit `1d6352b`, 2026-07-07.

Do NOT edit any file under `src/prosody/` in this repo. If a bug is found, fix it
upstream in klon first, then re-vendor this entire folder (and re-run this task's
regression tests) rather than patching torklon's copy directly.
